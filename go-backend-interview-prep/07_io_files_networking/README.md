# 07 -- IO, Files, and Networking

Reading, writing, and moving bytes around.

---

## Planned Topics

- [ ] io.Reader and io.Writer -- the fundamental abstractions
- [ ] Reading files -- `os.Open`, `os.ReadFile`
- [ ] Writing files -- `os.Create`, `os.WriteFile`
- [ ] Bufio -- buffered reading, `Scanner` for line-by-line
- [ ] String formatting -- `fmt.Sprintf`, format verbs
- [ ] Regular expressions -- `regexp` package basics
- [ ] JSON -- `encoding/json`, struct tags, Marshal/Unmarshal
- [ ] XML (brief) -- `encoding/xml`, similar patterns
- [ ] Time -- `time.Time`, formatting with reference time, durations
- [ ] TCP client/server -- `net.Dial`, `net.Listen`
- [ ] UDP basics -- datagram patterns
