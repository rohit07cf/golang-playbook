# 08 -- HTTP and Backend

Building web services with the standard library.

---

## Planned Topics

- [ ] HTTP server -- `http.ListenAndServe`, handlers
- [ ] HTTP client -- `http.Get`, custom clients, timeouts
- [ ] Routing -- `http.ServeMux`, path patterns
- [ ] Middleware -- handler wrapping, logging, auth
- [ ] JSON APIs -- request parsing, response encoding
- [ ] Request context -- `r.Context()`, passing values
- [ ] Graceful shutdown -- `server.Shutdown(ctx)`
- [ ] Structured logging -- `log/slog` (Go 1.21+)
- [ ] Environment config -- `os.Getenv`, config patterns
