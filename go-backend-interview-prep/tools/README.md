# Tools

Helper scripts and shared utilities for this repo.

---

## Planned Contents

- [ ] `run_all.sh` -- runs every example in order
- [ ] `test_all.sh` -- runs all tests with verbose output
- [ ] `lint.sh` -- runs `go vet` and `golangci-lint` on the repo
- [ ] Shared test helpers (if needed across multiple sections)
