# 01 -- Go Basics

Core syntax and building blocks of the language.

---

## Planned Topics

- [ ] Hello World -- entry point, `package main`, `func main()`
- [ ] Values -- basic types: int, float, bool, string
- [ ] Variables -- `var`, short declaration `:=`, zero values
- [ ] Constants -- `const`, iota, typed vs untyped
- [ ] For loops -- the only loop in Go, three forms
- [ ] If/Else -- conditionals, init statements
- [ ] Switch -- expression switch, type switch, no fallthrough by default
- [ ] Strings and Runes -- UTF-8, byte vs rune, iteration pitfalls
- [ ] Type conversions -- explicit conversion rules, no implicit casting
