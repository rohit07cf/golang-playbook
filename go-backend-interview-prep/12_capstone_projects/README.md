# 12 -- Capstone Projects

Full implementations that combine everything from previous sections.

---

## Planned Projects

- [ ] **HTTP API Service** -- CRUD endpoints, middleware stack, graceful shutdown, structured logging, JSON request/response, basic tests
- [ ] **Concurrent Pipeline** -- fan-out/fan-in data processing, context cancellation, error propagation, worker pool, rate limiting
- [ ] **CLI Tool** -- flag parsing, file I/O, exit codes, testable command structure, piped input support

Each project will include:

- Complete runnable code
- Tests
- A README explaining design decisions
- Interview talking points (what to highlight when describing the project)
